-- Key System with Script Chooser using Orion Library for Roblox
-- Place this LocalScript in StarterPlayerScripts

-- Wait until game and local player are loaded
repeat wait() until game:IsLoaded() and game.Players.LocalPlayer

-- Load Orion UI library from official GitHub repo
local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/jensonhirst/Orion/main/source"))()

-- Create UI Window
local Window = OrionLib:MakeWindow({
    Name = "ShiinaMahiruPublicScript (premium)",
    HidePremium = true,
    IntroText = "Nhập Key Đi nào!",
    SaveConfig = false,
})

-- Define the correct key values manually
local correctKeys = {
    "KEY_UPDATESOON",
    "KEY_FREE24H",
    "ShiinaMahiru"
}

local userKey = ""
local accessGranted = false

-- Create Unlock Tab (for key input)
local UnlockTab = Window:MakeTab({
    Name = "Unlock",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false,
})

-- Create Scripts Tab (initially disabled, will enable if access granted)
local ScriptsTab = Window:MakeTab({
    Name = "Scripts",
    Icon = "rbxassetid://6026568196",
    PremiumOnly = false,
})

-- Custom scripts definitions: only BananaHub loader
-- Custom scripts definitions: add your new script here
local availableScripts = {
    ["grow a garden OLD MENU Loader"] = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/ameicaa1/Grow-a-Garden/main/Grow_A_Garden.lua"))()
        OrionLib:MakeNotification({
            Name = "Grow a garden",
            Content = "Grow A garden v1",
            Image = "rbxassetid://4483345998",
            Time = 5,
        })
    end,
    ["Grow a garden NEW MENU V2"] = function()
        loadstring(game:HttpGet(('https://raw.githubusercontent.com/depthso/Grow-a-Garden/refs/heads/main/autofarm.lua')))() -- Replace with your script URL
        OrionLib:MakeNotification({
            Name = "New Script",
            Content = "Grow a garden v2",
            Image = "rbxassetid://4483345998",
            Time = 5,
        })
    end,
    ["Hakiyuu legend"] = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Xm2iufas09ijmF/sigma-hub/refs/heads/main/temp.lua", true))() -- Replace with your script URL
        OrionLib:MakeNotification({
            Name = "New Script",
            Content = "hakiyuu loaded",
            Image = "rbxassetid://4483345998",
            Time = 5,
        })
    end,
}

-- Key Input variable
local inputKey = ""

-- Unlock Tab: Add Textbox for key input
UnlockTab:AddTextbox({
    Name = "nhập key",
    Default = "",
    TextDisappear = true,
    Callback = function(text)
        inputKey = text
    end,
})

-- Unlock Tab: Add submit button
UnlockTab:AddButton({
    Name = "xác nhận Key",
    Callback = function()
        accessGranted = false  -- Reset access status
        for _, key in ipairs(correctKeys) do
            if inputKey == key then
                accessGranted = true
                break
            end
        end
        
        if accessGranted then
            OrionLib:MakeNotification({
                Name = "Access Granted",
                Content = "key whitelist check success , dùng đc r đó",
                Image = "rbxassetid://4483345998",
                Time = 5,
            })
        else
            OrionLib:MakeNotification({
                Name = "Access Denied",
                Content = "key sai r m dùng crack à?.",
                Image = "rbxassetid://4483345998",
                Time = 5,
            })
        end
        -- Clear input key
        inputKey = ""
    end,
})

-- Scripts Tab UI Elements
local selectedScript = nil

ScriptsTab:AddDropdown({
    Name = "chọn script",
    Default = nil,
    Options = (function()
        local keys = {}
        for k, _ in pairs(availableScripts) do
            table.insert(keys, k)
        end
        return keys
    end)(),
    Callback = function(choice)
        selectedScript = choice
    end,
})

ScriptsTab:AddButton({
    Name = "chạy Script",
    Callback = function()
        if not accessGranted then
            OrionLib:MakeNotification({
                Name = "Access Denied",
                Content = "nhập key mới chạy
